<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="item">
	<select id="selectItemCount" resultType="int">
	    SELECT COUNT(*)
	    FROM ITEM
	    <where>
	        <choose>
	            <when test="type == 'nowDeal'">
	                STATUS = 'N'
	            </when>
	            <when test="type == 'endDeal'">
	                STATUS = 'E'
	            </when>
	            <when test="type == 'yetDeal'">
	                STATUS = 'Y'
	            </when>
	            <when test="type == 'search'">
	                ITEM_TITLE LIKE '%' || #{keyword} || '%'
	            </when>
	        </choose>
	    </where>
	</select>
	<select id="selectItemList" resultType="item">
	    SELECT *
	    FROM (
	        SELECT ROWNUM RNUM, A.*
	        FROM (
	            SELECT *
	            FROM ITEM
	            <where>
	                <choose>
	                    <when test="type == 'nowDeal'">
	                        STATUS = 'N'
	                    </when>
	                    <when test="type == 'endDeal'">
	                        STATUS = 'E'
	                    </when>
	                    <when test="type == 'yetDeal'">
	                        STATUS = 'Y'
	                    </when>
	                    <when test="type == 'search' and keyword != null and keyword != ''">
	                        ITEM_TITLE LIKE '%' || #{keyword} || '%'
	                    </when>
	                </choose>
	            </where>
	            ORDER BY ITEM_NO DESC
	        ) A
	        WHERE ROWNUM <![CDATA[<=]]> #{offset} + #{limit}
	    )
	    WHERE RNUM > #{offset}
	</select>

	<insert id="insertItem"
		parameterType="com.kh.kbay.item.model.vo.Item">
		<selectKey keyProperty="itemNo" resultType="_int"
			order="BEFORE">
			SELECT SEQ_ITEM_NO.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO ITEM (
		ITEM_NO,
		ITEM_TITLE,
		ITEM_CONTENT,
		ITEM_SIZE,
		START_PRICE,
		DIRECT_BUY,
		BUY_NOW_PRICE,
		CURRENT_PRICE,
		END_TIME,
		ITEM_CD_NO,
		USER_NO, COUNTRY_NO, STATUS,
		START_TIME,
		CREATE_AT
		) VALUES (
		#{itemNo},
		#{itemTitle},
		#{itemContent},
		#{item_Size}, #{startPrice},
		#{directBuy},
		#{buyNowPrice}, #{startPrice}, TO_DATE(#{endTime},
		'YYYY-MM-DD"T"HH24:MI'),
		#{itemCdNo},
		#{userNo}, #{countryNo}, 'N',
		SYSDATE,
		SYSDATE
		)
	</insert>
	<insert id="insertItemImg"
		parameterType="com.kh.kbay.item.model.vo.ItemImg">
		INSERT INTO ITEM_IMG (
		ITEM_IMG_NO, ITEM_NO, IMG_URL
		)
		VALUES (
		SEQ_ITEM_IMG_NO.NEXTVAL, #{itemNo}, #{imgUrl}
		)
	</insert>
</mapper>