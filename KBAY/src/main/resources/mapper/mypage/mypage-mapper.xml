<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mypage">
	<select id="selectSummary" parameterType="int" resultType="MyPageSummary">
	    SELECT
	        (SELECT COUNT(*) FROM BID WHERE USER_NO = #{userNo}) AS bidCount,
	        (SELECT COUNT(*) FROM ITEM WHERE USER_NO = #{userNo}) AS sellCount,
	        (SELECT COUNT(*) FROM WISHLIST WHERE USER_NO = #{userNo}) AS wishCount
		FROM DUAL
	</select>
	
	<select id="selectAccidentCount" parameterType="int" resultType="int">
	    SELECT COUNT(*)
	    FROM REPORT r
	    WHERE
	        (
	            r.target_type = 'item'
	            AND EXISTS (
	                SELECT 1 FROM ITEM i
	                WHERE i.item_no = r.target_no
	                AND i.user_no = #{userNo}
	            )
	        )
	        OR
	        (
	            r.target_type = 'board'
	            AND EXISTS (
	                SELECT 1 FROM BOARD_POST b
	                WHERE b.board_no = r.target_no
	                AND b.user_no = #{userNo}
	            )
	        )
	        OR
	        (
	            r.target_type = 'reply'
	            AND EXISTS (
	                SELECT 1 FROM REPLY rp
	                WHERE rp.reply_no = r.target_no
	                AND rp.user_no = #{userNo}
	            )
	        )
	</select>
</mapper>