<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="wish">

    <select id="checkWishlist" resultType="_int">
        SELECT COUNT(*) 
        FROM WISHLIST 
        WHERE USER_NO = #{userNo} AND ITEM_NO = #{itemNo}
    </select>

    <insert id="insertWishlist">
        INSERT INTO TEAM.WISHLIST (USER_NO, ITEM_NO, WISH_DATE)
        VALUES (#{userNo}, #{itemNo}, DEFAULT)
    </insert>

    <delete id="deleteWishlist">
        DELETE FROM TEAM.WISHLIST 
        WHERE USER_NO = #{userNo} AND ITEM_NO = #{itemNo}
    </delete>

</mapper>